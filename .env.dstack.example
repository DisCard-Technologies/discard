# DisCard dstack Environment Configuration
# Copy this file to .env.dstack and fill in your values
#
# Usage:
#   cp .env.dstack.example .env.dstack
#   # Edit .env.dstack with your values
#   docker-compose -f docker-compose.dstack.yaml --env-file .env.dstack up -d

# =============================================================================
# Phala Confidential AI (Required)
# =============================================================================
# Get your API key from https://redpill.ai
PHALA_AI_API_KEY=your-phala-api-key-here
PHALA_AI_API_URL=https://api.redpill.ai/v1
LLM_MODEL=meta-llama/llama-3.3-70b-instruct

# =============================================================================
# Solana Configuration (Required)
# =============================================================================
# Use a reliable RPC endpoint (Helius, QuickNode, Triton, etc.)
SOLANA_RPC_URL=https://mainnet.helius-rpc.com/?api-key=your-helius-key
SOLANA_WS_URL=wss://mainnet.helius-rpc.com/?api-key=your-helius-key

# =============================================================================
# Turnkey Configuration (Required for wallet operations)
# =============================================================================
# Get credentials from https://app.turnkey.com
TURNKEY_API_PUBLIC_KEY=your-turnkey-public-key
TURNKEY_API_PRIVATE_KEY=your-turnkey-private-key
TURNKEY_ORGANIZATION_ID=your-turnkey-org-id

# =============================================================================
# Range Compliance (Optional - for AML/compliance checks)
# =============================================================================
RANGE_API_KEY=your-range-api-key
RANGE_API_URL=https://api.range.org

# =============================================================================
# Price Feed Configuration (Optional - defaults provided)
# =============================================================================
JUPITER_API_URL=https://price.jup.ag/v6
PYTH_PRICE_FEED_URL=https://hermes.pyth.network
BIRDEYE_API_KEY=your-birdeye-api-key

# =============================================================================
# TLS Configuration (Optional - for mTLS between services)
# =============================================================================
ENABLE_TLS=false
# TLS_CERT=/path/to/cert.pem
# TLS_KEY=/path/to/key.pem
# TLS_CA=/path/to/ca.pem

# =============================================================================
# TEE Configuration
# =============================================================================
TEE_ENABLED=true
